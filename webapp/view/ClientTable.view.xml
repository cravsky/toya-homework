<mvc:View
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    controllerName="sap.ui.demo.toya.controller.ClientTable">
    <Page title="Klienci">
        <Table id="idCustomersTable" items="{/Customers}">
            <headerToolbar>
                <Toolbar>
                    <Title text="Lista klientów" />
                    <ToolbarSpacer />
                    <SearchField placeholder="Filtruj według miasta..." liveChange="onFilterCity" width="20%"/>
                </Toolbar>
            </headerToolbar>
            <columns>
                <!-- Sortable Company Name column -->
                <Column hAlign="Left">
                    <header>
                        <HBox alignItems="Center">
                            <Label text="Nazwa firmy" />
                            <Button icon="sap-icon://sort" press="onSortCompanyName" type="Transparent"/>
                        </HBox>
                    </header>
                </Column>
                <Column>
                    <Text text="Adres" />
                </Column>
                <Column>
                    <Text text="Miasto" />
                </Column>
                <Column>
                    <Text text="Kod pocztowy" />
                </Column>
                <Column>
                    <Text text="Numer telefonu" />
                </Column>
            </columns>
            <items>
                <ColumnListItem>
                    <cells>
                        <Text text="{CompanyName}" />
                        <!-- Nazwa firmy -->
                        <Text text="{Address}" />
                        <!-- Adres -->
                        <Text text="{City}" />
                        <!-- Miasto -->
                        <Text text="{PostalCode}" />
                        <!-- Kod pocztowy -->
                        <Text text="{Phone}" />
                        <!-- Numer telefonu -->
                    </cells>
                </ColumnListItem>
            </items>
        </Table>
    </Page>
</mvc:View>